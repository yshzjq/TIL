---
title: 1-0강 오프닝 - 딥러닝 기초 학습 로드맵
date: 2026-08-19
updated: 2026-08-19
description: KANT 강의 '1-0강 오프닝 - 딥러닝 기초 학습 로드맵' 정리
---


# 1. 이번에 우리가 배울 것

 데이터를 준비하고, 모델을 만들고, 학습시키고, 검증하고, 저장하고, 다시 추론하는 전체 흐름을 직접 이해하는 것
 
어떤 문제는 규칙 몇 개만으로도 해결할 수 있습니다. 어떤 문제는 전통적인 머신러닝 모델이 더 적절합니다. 어떤 문제는 딥러닝이 큰 장점을 가집니다.

이번 시간의 핵심 질문은 다음과 같습니다.

```
"이 문제는 딥러닝으로 풀어야 할까요?"
```

---

## 2. 규칙 기반, 머신러닝, 딥러닝 접근 비교

예를 들어 고객 문의 메시지를 보고 “환불 문의인지 아닌지” 분류한다고 생각해봅시다.

### 2-1. 규칙 기반 접근

```python
text = "상품을 반품하고 환불받고 싶어요."

if "환불" in text or "반품" in text:
    label = "환불 문의"
else:
    label = "기타 문의"

print(label)
```

출력은 다음과 같습니다.

```
환불 문의
```

문장에 `환불`과 `반품`이 모두 들어 있지만 `or` 조건은 하나만 참이어도 전체가 참이므로 `환불 문의`가 출력된다.

규칙 기반 접근은 사람이 직접 조건을 작성.

장점은 단순하고 빠르지만 표현이 조금만 바뀌면 놓치기 쉽습니다.


### 2-2. 머신러닝 접근

머신러닝에서는 사람이 특징을 설계합니다.

| 특징 | 예시 |
| --- | --- |
| 특정 단어 포함 여부 | 환불, 취소, 반품 포함 여부 |
| 문장 길이 | 글자 수, 단어 수 |
| 문의 카테고리 | 배송, 결제, 상품, 계정 |
| 이전 문의 횟수 | 고객의 과거 문의 수 |

모델은 이 특징들을 보고 분류 규칙을 학습


### 2-3. 딥러닝 접근

딥러닝에서는 모델이 데이터 안에서 유용한 특징 표현을 스스로 학습합니다.

```
입력 데이터
-> 신경망 모델
-> 내부 표현 학습
-> 예측 결과
```

텍스트 분류를 예로 들면, 딥러닝 모델은 단어 하나만 보는 것이 아니라 문장 전체의 패턴, 단어 순서, 문맥을 활용할 수 있습니다.





```python
RandomForestClassifier(
    n_estimators=100,   # 트리수
    max_depth=5         # 최대 깊이
)
```

